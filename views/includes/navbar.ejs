<nav class="navbar navbar-expand-lg sticky-top navbar-custom shadow-sm">
  <div class="container-fluid px-4 align-items-center">

    <!-- Brand -->
    <a class="navbar-brand d-flex align-items-center gap-2" href="/listings">
      <img src="/images/logo.png" alt="logo" style="height: 40px; border-radius: 8px;">
      <span class="fw-bold fs-4 brand-text" style="color: #ff385c;">WanderStay</span>
    </a>

    <!-- Center Search Bar -->
    <form class="navbar-search mx-auto d-none d-lg-flex" action="/listings" method="GET">
      <input
        type="text"
        name="q"
        class="search-input"
        placeholder="Search destinations"
      />
      <button class="search-btn" type="submit">
        <i class="fa-solid fa-magnifying-glass"></i>
        <span>Search</span>
      </button>
    </form>

    <!-- Toggler -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Right Links -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav ms-auto align-items-center gap-3 mt-3 mt-lg-0">

        <a class="nav-link nav-item-custom" href="/listings/new">
          WanderStay your home
        </a>

        <% if(!currUser) { %>
          <a class="btn btn-outline-danger rounded-pill px-4 btn-hover" href="/login">
            Sign In
          </a>
          <a class="btn btn-danger rounded-pill px-4 btn-hover" href="/signup">
            Sign Up
          </a>
        <% } else { %>
          <a class="btn btn-outline-dark rounded-pill px-4 btn-hover" href="/logout">
            Logout
          </a>
        <% } %>

      </div>
    </div>

  </div>
</nav>
